<div ng-controller="userCtrl" class="row admin-user">

    <h3>Users management</h3>

    <table st-table="displayedCollection" st-safe-src="rowCollection" class="table table-striped">
        <thead>
        <tr>
            <th st-sort="id">Id</th>
            <th st-sort="login">Login</th>
            <th st-sort="email">Email</th>
            <th st-sort="status">Status</th>
            <th st-sort="role">Role</th>
<!--            <th st-sort="name">Name</th>-->

            <th>Banned</th>
        </tr>
        <tr>
            <th colspan="6"><input st-search="" class="form-control" placeholder="Search ..." type="text"/></th>
        </tr>
        </thead>
        <tbody>
        <tr ng-repeat="row in displayedCollection">
            <td>{{row.id}}</td>
            <td>{{row.login}}</td>
            <td>{{row.email}}</td>
            <td>{{row.status}}</td>
            <td>{{row.role}}</td>
            <td>
<!--                <input name="remember" ng-change="ban(row)" ng-model="row.banned" class="form-control" type="checkbox"/>-->
            </td>
        </tr>
        </tbody>
    </table>
</div>

<script src="/application/assets/js/adminUsers.js"></script>